<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Result</title>
</head>

<body>
    <h1>Your Results</h1>
    <p>Score: <%= score %> out of <%= totalQuestions %></p>
    <ul>
        <% feedback.forEach((item, index) => { %>
            <li>
                Question <%= index + 1 %>: 
                <br>
                <strong>Your Answer:</strong> <%= item.userAnswer %> 
                <% if (item.isCorrect) { %>
                    - Correct!
                <% } else { %>
                    - Incorrect. The correct answer was: <%= item.correctAnswer %>
                <% } %>
            </li>
        <% }); %>
    </ul>
    <a href="/">Try Again</a>

</body>

</html>